<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nmrapp.nmrtutor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/chalkboardicon_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/chalkboardicon_2"
        android:supportsRtl="true"
        android:theme="@style/NMRTheme">
        <activity
            android:name=".MainActivity"
            android:icon="@drawable/chalkboardicon_2"
            android:theme="@style/NMRTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EasyProblemHomeActivity" />
        <activity
            android:name=".ProblemActivity"
            android:label="@string/title_activity_problem_set"
            android:theme="@style/NMRTheme.NoActionBar" />
    </application>

</manifest>